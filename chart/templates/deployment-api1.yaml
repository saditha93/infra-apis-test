apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "flask-two-apis.fullname" . }}-api1
  labels:
    app.kubernetes.io/name: {{ include "flask-two-apis.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: api1
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "flask-two-apis.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: api1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "flask-two-apis.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: api1
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      {{- range .Values.imagePullSecrets }}
        - name: {{ .name }}
      {{- end }}
      {{- end }}
      containers:
        - name: api1
          image: {{ .Values.imageApi1 | quote }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8001
          env:
            - name: SERVICE_NAME
              value: {{ .Values.env.SERVICE_NAME_API1 | quote }}
          resources:
{{- toYaml .Values.resources | nindent 12 }}
